(() => {
  'use strict';
  const M=window.SeyeModel, $=s=>document.querySelector(s), STORAGE='seye-operations-prototype-v1';
  let state;try{state=JSON.parse(localStorage.getItem(STORAGE));if(state?.version!==1)state=null;}catch{} state=state||M.seed();
  const views=['overview','orders','shipments','inventory','billing','dispatch','connections'];
  let view=views.includes(location.hash.slice(1))?location.hash.slice(1):'overview',business='all',query='',tab='all',sort='newest',lastFocus=null,toastTimer;
  const names={overview:'Overview',orders:'Orders',shipments:'Shipments',inventory:'Inventory',billing:'Logistics billing',dispatch:'Dispatch',connections:'Connections'};
  const paths={grid:'M3 3h7v7H3z M14 3h7v7h-7z M3 14h7v7H3z M14 14h7v7h-7z',orders:'M6 3h12v18l-3-2-3 2-3-2-3 2z M9 8h6 M9 12h6',ship:'M4 17l-2-6 10-4 10 4-2 6 M7 9V4h10v5 M9 4V2h6v2 M2 20c2 2 4 2 6 0 2 2 4 2 6 0 2 2 4 2 6 0',box:'M3 7l9-5 9 5v10l-9 5-9-5z M3 7l9 5 9-5 M12 12v10 M7.5 4.5l9 5v4',invoice:'M5 3h14v18l-3-2-4 2-4-2-3 2z M8 8h8 M8 12h5 M8 16h3',truck:'M1 4h13v13H1z M14 9h4l4 4v4h-8 M7 18a2 2 0 11-4 0 2 2 0 014 0 M20 18a2 2 0 11-4 0 2 2 0 014 0',link:'M10 13a5 5 0 007 0l3-3a5 5 0 00-7-7l-2 2 M14 11a5 5 0 00-7 0l-3 3a5 5 0 007 7l2-2',search:'M21 21l-5-5 M18 10a8 8 0 11-16 0 8 8 0 0116 0',bell:'M18 8a6 6 0 00-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9 M10 21h4',chevron:'M9 5l7 7-7 7',down:'M6 9l6 6 6-6',arrow:'M5 12h14 M14 7l5 5-5 5',plus:'M12 5v14 M5 12h14',check:'M5 12l4 4L19 6',checkCircle:'M20 11v1a8 8 0 11-5-7 M8 11l4 4 9-10',clock:'M12 8v5l3 2 M21 12a9 9 0 11-18 0 9 9 0 0118 0',lock:'M5 10h14v11H5z M8 10V6a4 4 0 018 0v4 M12 14v3',refresh:'M20 7a9 9 0 00-15-2L2 8 M2 2v6h6 M4 17a9 9 0 0015 2l3-3 M22 22v-6h-6',calendar:'M4 5h16v16H4z M4 10h16 M8 2v6 M16 2v6',download:'M12 3v12 M7 10l5 5 5-5 M4 16v5h16v-5',close:'M6 6l12 12 M18 6L6 18',info:'M12 11v6 M12 7v.1 M21 12a9 9 0 11-18 0 9 9 0 0118 0',wallet:'M3 5h15v3 M3 5v15h18V8H3 M16 12h5v4h-5z',menu:'M3 6h18 M3 12h18 M3 18h18',filter:'M4 7h16 M7 12h10 M10 17h4',more:'M5 12h.1 M12 12h.1 M19 12h.1',logout:'M9 3H3v18h6 M10 12h12 M17 7l5 5-5 5',help:'M9 8a3 3 0 116 0c0 3-3 2-3 5 M12 17v.1 M21 12a9 9 0 11-18 0 9 9 0 0118 0'};
  const icon=(name,cls='')=>`<svg class="icon ${cls}" aria-hidden="true" viewBox="0 0 24 24"><path d="${paths[name]||paths.box}"/></svg>`;
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const money=n=>'₦'+Number(n||0).toLocaleString('en-NG',{maximumFractionDigits:0});
  const all=()=>state.orders.filter(o=>business==='all'||o.business===business);
  const get=id=>state.orders.find(o=>o.id===id), product=o=>state.products.find(p=>p.id===o.sku);
  const btn=(label,action,id='',kind='',ico='')=>`<button type="button" class="btn ${kind}" data-action="${action}" ${id?`data-id="${id}"`:''}>${ico?icon(ico):''}${label}</button>`;
  function badge(label){const green=['Paid','Product paid','Ready to dispatch','Received','Arrived','Verified','Allocated'];const amber=['Not assessed','Awaiting payment','Partially paid','Awaiting stock','On hold','Delivery not quoted'];const blue=['In transit','Draft invoice','Dispatched'];return `<span class="status ${green.includes(label)?'green':amber.includes(label)?'amber':blue.includes(label)?'blue':'gray'}">${esc(label)}</span>`;}
  function thumb(p){let shape=p.shape==='pot'?`<ellipse cx="24" cy="40" rx="18" ry="3" fill="#2032460c"/><path d="M11 23h27l-2 14q-11 7-23 0z" fill="${p.color}"/><path d="M9 23h31 M12 19q12-9 24 0" stroke="#657369" stroke-width="2" fill="none"/><rect x="20" y="13" width="8" height="3" rx="2" fill="#66706a"/><path d="M11 26H6v7h5 M38 26h5v7h-6" fill="none" stroke="#718276" stroke-width="2"/>`:p.shape==='bowl'?`<path d="M7 21h35q-3 21-18 21T7 21" fill="${p.color}" opacity=".6"/><ellipse cx="24" cy="21" rx="17" ry="5" fill="#dde8eb" stroke="${p.color}"/><path d="M12 17h25q-3 15-13 15T12 17" fill="#c5d6dd" opacity=".8"/><ellipse cx="24" cy="17" rx="12" ry="4" fill="#edf2f4"/>`:p.shape==='shoe'?`<path d="M7 31q9 2 14-18l8 5 4 13 10 5q1 5-9 5H8q-6-2-1-10" fill="${p.color}"/><path d="M25 19l6 12 M32 37l-1 9" stroke="#907257" stroke-width="3"/><path d="M10 29l11-16" stroke="#dbc8b4" stroke-width="4"/>`:p.shape==='bag'?`<rect x="9" y="17" width="31" height="25" rx="5" fill="${p.color}"/><path d="M17 19V12a8 8 0 0116 0v7" fill="none" stroke="#7c5d48" stroke-width="3"/><path d="M9 24h31" stroke="#7c5d48"/><rect x="21" y="22" width="7" height="5" rx="1" fill="#dbbe86"/>`:`<rect x="13" y="5" width="24" height="39" rx="3" fill="${p.color}"/><rect x="16" y="17" width="18" height="16" fill="#e8eff3"/><circle cx="21" cy="21" r="2" fill="#cf8e8e"/><circle cx="29" cy="21" r="2" fill="#709dce"/><path d="M17 37h16" stroke="#8b9ba8"/>`;return `<span class="thumb"><svg viewBox="0 0 48 48" aria-hidden="true">${shape}</svg></span>`;}
  function save(){try{localStorage.setItem(STORAGE,JSON.stringify(state));}catch{toast('Browser storage is unavailable. Changes last for this session.');}}
  function toast(message){clearTimeout(toastTimer);$('#toast').textContent=message;$('#toast').classList.add('visible');toastTimer=setTimeout(()=>$('#toast').classList.remove('visible'),4000);}
  function go(v,t='all'){query='';tab=t;view=v;location.hash=v;render();}
  function heading(title,subtitle,actions=''){return `<div class="page-heading"><div><h1>${title}</h1><p class="subtitle">${subtitle}</p></div><div class="heading-buttons">${actions}</div></div>`;}
  function nav(){const items=[['overview','grid'],['orders','orders'],['shipments','ship'],['inventory','box'],['billing','invoice'],['dispatch','truck']];return items.map(([v,i])=>`<button class="nav-item ${view===v?'active':''}" data-action="nav" data-id="${v}" ${view===v?'aria-current="page"':''}>${icon(i)}${names[v]}${v==='orders'?`<span class="count">${all().filter(o=>!o.dispatched).length}</span>`:v==='dispatch'?`<span class="count">${all().filter(M.ready).length}</span>`:''}</button>`).join('');}
  function stats(){const os=all(),out=os.filter(o=>o.invoiced&&M.assessed(o)).reduce((s,o)=>s+M.balance(o),0);return `<div class="stats">${[
    ['Open orders',os.filter(o=>!o.dispatched).length,'Across '+(business==='all'?'3 businesses':'this business'),'orders','orders'],
    ['Logistics outstanding',money(out),os.filter(o=>o.invoiced&&M.balance(o)>0).length+' invoices awaiting payment','wallet','billing'],
    ['Charges to assess',os.filter(o=>!M.assessed(o)).length,'Actual costs billed after arrival','invoice','billing'],
    ['Ready to dispatch',os.filter(M.ready).length,'<span class="good">All payments verified</span>','truck','dispatch']
    ].map(([label,value,note,i,v])=>`<button class="stat" data-action="stat" data-id="${v}" data-filter="${label==='Charges to assess'?'unassessed':'all'}"><span class="stat-label">${label}${icon(i)}</span><span class="stat-value">${value}</span><span class="stat-note">${note}</span></button>`).join('')}</div>`;}
  function customer(o){return `<span class="customer-cell"><span class="avatar" style="background:${o.business==='fashion'?'#f3eaf4':o.business==='kitchen'?'#eaf3ef':'#edf0f8'}">${o.initials}</span><span><strong>${esc(o.name)}</strong><small>${esc(o.city)}</small></span></span>`;}
  function orderTable(os,compact=false){if(!os.length)return empty('No orders in this view','Try another status, business, or search term.');return `<div class="table-wrap"><table class="${compact?'compact':''}"><thead><tr><th>Order ${icon('down','small')}</th><th>Customer</th>${!compact?'<th>Product</th><th>Business</th>':''}<th>Product payment</th><th>Logistics</th>${!compact?'<th>Balance</th>':''}<th></th></tr></thead><tbody>${os.map(o=>`<tr><td><button class="order-link" data-action="order" data-id="${o.id}">#${o.id}</button><small class="desktop-table-note">${o.batch}</small></td><td>${customer(o)}</td>${!compact?`<td><div class="product-cell">${thumb(product(o))}<span><strong>${product(o).name}</strong><small>${product(o).variant}</small></span></div></td><td><span class="business-dot ${o.business}"></span>${M.businesses[o.business].replace('Seye ','')}</td>`:''}<td><span class="money">${money(o.productPaid)}</span><small class="desktop-table-note number-good">Paid in Shopify</small></td><td>${badge(M.status(o))}</td>${!compact?`<td class="money">${M.assessed(o)?money(M.balance(o)):'<span class="muted-dash">Not assessed</span>'}</td>`:''}<td><button class="icon-button" aria-label="Open order ${o.id}" data-action="order" data-id="${o.id}">${icon('chevron','small')}</button></td></tr>`).join('')}</tbody></table></div>`;}
  const empty=(title,desc)=>`<div class="empty">${icon('search')}<h3>${title}</h3><p>${desc}</p></div>`;
  function overview(){
    const os=all(),batch=state.batches.find(b=>os.some(o=>o.batch===b.id))||state.batches[0],ready=os.filter(M.ready),due=os.filter(o=>o.invoiced&&M.balance(o)>0),unassessed=os.filter(o=>o.batch===batch.id&&!M.assessed(o));
    const tasks=[];
    if(unassessed.length&&batch.status!=='In transit')tasks.push(`<div class="attention-row"><span class="attention-icon">${icon('invoice')}</span><div><h3>${batch.finalized?'Import costs assessed':batch.name+' is ready for cost review'}</h3><p>${batch.finalized?'Review customer charges and add delivery quotes.':`${unassessed.length} orders need their final import charges.`}</p></div>${btn(batch.finalized?'View charges':'Review costs',batch.finalized?'billing-unassessed':'batch',batch.id,'small','arrow')}</div>`);
    if(due.length)tasks.push(`<div class="attention-row"><span class="attention-icon blue">${icon('wallet')}</span><div><h3>${due.length} logistics invoices awaiting payment</h3><p>${money(due.reduce((s,o)=>s+M.balance(o),0))} outstanding. These orders are on hold.</p></div>${btn('View invoices','billing-unpaid','','small','arrow')}</div>`);
    if(ready.length)tasks.push(`<div class="attention-row"><span class="attention-icon green">${icon('truck')}</span><div><h3>${ready.length} orders cleared for dispatch</h3><p>Stock received. Product and logistics payments verified.</p></div>${btn('Open dispatch','nav','dispatch','small','arrow')}</div>`);
    return `${heading('Operations overview','Your orders, shipments and payments, in one place.',`<span class="date-label desktop-only">${icon('calendar','small')}13 Sep 2026</span>${btn('View orders','nav','orders','','orders')}`)}${stats()}
    <div class="overview-grid"><div class="overview-main"><section class="card"><div class="card-header"><h2>Needs your attention <span class="text-badge">${tasks.length} tasks</span></h2>${icon('clock','small')}</div><div class="action-list">${tasks.length?tasks.join(''):empty('No actions needed right now',batch.status==='In transit'?'Your shipment is in transit. Actual costs will be reviewed after arrival.':'There are no outstanding actions for this business.')}</div></section>
    <section class="card gap-top"><div class="card-header"><div><h2>Recent orders</h2><p>Product payment and logistics clearance are tracked separately.</p></div>${btn('View all','nav','orders','text','arrow')}</div>${orderTable(os.slice(0,6),true)}<div class="table-footer"><span>Showing ${Math.min(os.length,6)} of ${os.length} orders</span><span>Shopify sample data</span></div></section></div>
    <aside class="overview-aside"><section class="card shipment-card"><div class="eyeline"><span>Active shipment</span>${badge(batch.status)}</div><h2>${batch.name}</h2><p class="shipment-id">${batch.id} · Sea freight</p><div class="route"><div class="route-city">${batch.origin}<small>China</small></div><div class="route-line">${icon('ship')}</div><div class="route-city">${batch.destination}<small>Nigeria</small></div></div><div class="container-art" aria-hidden="true"><div class="cargo"></div><div class="cargo second"><small>SEYE</small></div></div><div class="shipment-meta"><div><small>${batch.status==='In transit'?'Expected':'Arrived'}</small><strong>${batch.date}</strong></div><div><small>${batch.status==='In transit'?'Incoming':'Received'}</small><strong>${batch.units} units</strong></div><div><small>Sample orders</small><strong>${state.orders.filter(o=>o.batch===batch.id).length}</strong></div></div>${btn('View shipment','batch',batch.id,'','arrow')}</section><section class="card activity"><div class="card-header"><h2>Workspace activity</h2>${icon('clock','small')}</div><div class="activity-list">${activityMarkup(3)}</div></section></aside></div><p class="footnote">${icon('lock')}Dispatch stays on hold until all required charges are finalized and paid.</p>`;
  }
  function activityMarkup(limit){return state.activity.slice(0,limit).map(a=>`<div class="activity-row"><span class="activity-symbol">${icon(a.type||'check')}</span><div><p>${esc(a.text)}</p><small>${esc(a.detail)}</small><small>${a.time}</small></div></div>`).join('');}
  function filterOrders(){let os=all().filter(o=>`${o.id} ${o.name} ${o.city} ${product(o).name} ${o.batch}`.toLowerCase().includes(query.toLowerCase()));if(tab==='unassessed')os=os.filter(o=>!M.assessed(o));if(tab==='unpaid')os=os.filter(o=>o.invoiced&&M.balance(o)>0);if(tab==='ready')os=os.filter(M.ready);if(tab==='dispatched')os=os.filter(o=>o.dispatched);if(sort==='balance')os.sort((a,b)=>M.balance(b)-M.balance(a));else if(sort==='name')os.sort((a,b)=>a.name.localeCompare(b.name));return os;}
  function tabsMarkup(options){return `<div class="tabs" role="tablist" aria-label="Order status">${options.map(([id,label,count])=>`<button role="tab" aria-selected="${tab===id}" class="tab ${tab===id?'active':''}" data-action="tab" data-id="${id}">${label}${count!==undefined?`<span>${count}</span>`:''}</button>`).join('')}</div>`;}
  function filterMarkup(placeholder='Search order, customer or product'){return `<div class="filters"><label class="search-field">${icon('search','small')}<input id="table-search" aria-label="Search this table" placeholder="${placeholder}" value="${esc(query)}"></label><select id="table-sort" aria-label="Sort orders"><option value="newest" ${sort==='newest'?'selected':''}>Newest first</option><option value="balance" ${sort==='balance'?'selected':''}>Highest balance</option><option value="name" ${sort==='name'?'selected':''}>Customer name</option></select></div>`;}
  function orders(){const os=all();return `${heading('Orders','Follow every preorder from product payment to final delivery.',btn('Export orders','export','','','download'))}<section class="card">${tabsMarkup([['all','All orders',os.length],['unassessed','Not assessed',os.filter(o=>!M.assessed(o)).length],['unpaid','Awaiting payment',os.filter(o=>o.invoiced&&M.balance(o)>0).length],['ready','Ready',os.filter(M.ready).length],['dispatched','Dispatched',os.filter(o=>o.dispatched).length]])}${filterMarkup()}<div id="table-results">${orderTable(filterOrders())}</div><div class="table-footer"><span id="result-count">${filterOrders().length} orders</span><span>All amounts in NGN</span></div></section>`;}
  function shipments(){const bs=state.batches.filter(b=>business==='all'||all().some(o=>o.batch===b.id));return `${heading('Shipments','Purchase batches, arrivals and actual import costs.',business==='all'||business==='import'?btn('Review arrived shipment','batch','SHP-2606','primary','invoice'):'')}<div class="shipment-grid">${bs.map(b=>`<section class="card batch-card"><div class="batch-top">${icon('ship')}${badge(b.status)}</div><h2>${b.name}</h2><p class="muted" style="font-size:10px;margin-top:5px">${b.id} · Sea freight</p><div class="route"><div class="route-city">${b.origin}<small>${b.depart} 2026</small></div><div class="route-line">${icon('ship')}</div><div class="route-city">${b.destination}<small>${b.arrive} 2026</small></div></div><dl class="definition-grid"><div><dt>Batch quantity</dt><dd>${b.units} units</dd></div><div><dt>Extra stock</dt><dd>${b.extraUnits} units</dd></div><div><dt>Import costs</dt><dd>${b.finalized?money(b.freight+b.clearance):'Not finalized'}</dd></div><div><dt>Cost review</dt><dd>${b.finalized?'Approved':b.status==='In transit'?'Awaiting arrival':'Ready for review'}</dd></div></dl>${btn(b.finalized?'View cost breakdown':b.status==='In transit'?'View shipment':'Review actual costs','batch',b.id,b.status==='Arrived'?'primary':'','arrow')}</section>`).join('')}</div><div class="card help-card"><h3>Costs follow the shipment</h3><p>Enter freight and clearance costs after they are confirmed. Extra units keep their share of the cost; customers are billed for their allocated items. Local delivery is quoted separately for each order.</p><span class="status gray">Demo allocation method: chargeable weight</span></div>`;}
  function inventory(){const ps=state.products.filter(p=>business==='all'||p.business===business);return `${heading('Inventory','A shared stock view, with clear ownership across your businesses.')}<div class="stats">${[['Received stock',ps.reduce((s,p)=>s+p.stock,0)],['Reserved for orders',ps.reduce((s,p)=>s+p.reserved,0)],['Available to sell',ps.reduce((s,p)=>s+p.stock-p.reserved,0)],['Incoming',ps.reduce((s,p)=>s+p.incoming,0)]].map(([s,n])=>`<div class="stat"><span class="stat-label">${s}${icon('box')}</span><span class="stat-value">${n}</span><span class="stat-note">Units across ${ps.length} product variants</span></div>`).join('')}</div><section class="card"><div class="card-header"><h2>Stock by product</h2><span class="status gray">Lagos warehouse</span></div><div class="table-wrap"><table><thead><tr><th>Product / variant</th><th>Business</th><th>SKU</th><th>Received stock</th><th>Reserved</th><th>Available</th><th>Incoming</th></tr></thead><tbody>${ps.map(p=>`<tr><td><div class="product-cell">${thumb(p)}<span><strong>${p.name}</strong><small>${p.variant}</small></span></div></td><td><span class="business-dot ${p.business}"></span>${M.businesses[p.business]}</td><td class="muted">${p.id}</td><td class="money">${p.stock}</td><td>${p.reserved}</td><td><strong class="number-good">${p.stock-p.reserved}</strong><div class="stock-bar"><span style="width:${(p.stock-p.reserved)/p.stock*100}%"></span></div></td><td class="muted">${p.incoming}</td></tr>`).join('')}</tbody></table></div><div class="table-footer"><span>${ps.length} variants</span><span>Incoming units are excluded from available stock.</span></div></section>`;}
  function billingTable(os){if(!os.length)return empty('No matching logistics charges','Change the filter to see other customer balances.');return `<div class="table-wrap"><table><thead><tr><th>Customer / order</th><th>Importation</th><th>Local delivery</th><th>Paid</th><th>Outstanding</th><th>Status</th><th></th></tr></thead><tbody>${os.map(o=>`<tr><td>${customer(o)}<button class="order-link" style="margin:5px 0 0 38px;font-size:10px" data-action="order" data-id="${o.id}">#${o.id}</button></td><td class="money">${o.importFee===null?'<span class="muted-dash">Not assessed</span>':money(o.importFee)}</td><td class="money">${o.delivery===null?'<span class="muted-dash">Not quoted</span>':money(o.delivery)}</td><td class="money">${money(o.paid)}</td><td class="money">${M.assessed(o)?money(M.balance(o)):'<span class="muted-dash">—</span>'}</td><td>${badge(M.status(o))}</td><td>${o.importFee===null?btn('Review batch','batch',o.batch,'small'):!o.invoiced?btn('Create invoice','invoice',o.id,'small'):btn('View invoice','invoice',o.id,'small')}</td></tr>`).join('')}</tbody></table></div>`;}
  function billing(){const os=all();return `${heading('Logistics billing','Actual importation and delivery charges, collected after arrival.',business==='all'||business==='import'?btn('Review batch costs','batch','SHP-2606','primary','invoice'):btn('View shipments','nav','shipments','','ship'))}<div class="callout">${icon('info')}<p><strong>Product payments are already collected in Shopify.</strong> Only finalized logistics charges appear in the outstanding balance. Unassessed charges keep dispatch on hold.</p></div><section class="card">${tabsMarkup([['all','All charges',os.length],['unassessed','Not assessed',os.filter(o=>!M.assessed(o)).length],['unpaid','Awaiting payment',os.filter(o=>o.invoiced&&M.balance(o)>0).length],['ready','Cleared',os.filter(M.ready).length]])}${filterMarkup()}<div id="table-results">${billingTable(filterOrders())}</div><div class="table-footer"><span id="result-count">${filterOrders().length} orders</span><span>NGN · Customer charges</span></div></section>`;}
  function checks(o){return [[true,'Product payment verified'],[M.assessed(o),M.assessed(o)?'Import and delivery charges finalized':'Logistics charges not finalized'],[o.invoiced&&M.assessed(o)&&M.balance(o)===0,M.assessed(o)&&M.balance(o)===0?'Logistics payment verified':M.assessed(o)?money(M.balance(o))+' logistics balance outstanding':'Logistics payment pending'],[o.received,o.received?'Stock received and allocated':'Stock awaiting arrival'],[!o.otherHold&&!o.quoteExpired,!o.otherHold&&!o.quoteExpired?'No other blocking holds':'A separate hold requires review']].map(([ok,label])=>`<div class="check-row ${ok?'':'fail'}">${icon(ok?'checkCircle':'lock')}<span>${label}</span></div>`).join('');}
  function dispatchView(){let os=all();const ready=os.filter(M.ready),blocked=os.filter(o=>!M.ready(o)&&!o.dispatched),sent=os.filter(o=>o.dispatched);let shown=tab==='blocked'?blocked:tab==='dispatched'?sent:ready;return `${heading('Dispatch','Only fully cleared orders reach the packing queue.',`<span class="status green">${icon('lock','small')}Payment clearance enforced</span>`)}<section class="card" style="margin-bottom:20px">${tabsMarkup([['all','Ready to dispatch',ready.length],['blocked','On hold',blocked.length],['dispatched','Dispatched',sent.length]])}</section><div class="dispatch-grid">${shown.map(o=>`<section class="card dispatch-card"><div class="batch-top"><button class="order-link" data-action="order" data-id="${o.id}">#${o.id}</button>${badge(M.status(o))}</div>${customer(o)}<div class="product-cell">${thumb(product(o))}<span><strong>${product(o).name}</strong><small>${o.quantity} ${o.quantity===1?'item':'items'} · ${o.batch}</small></span></div><div class="checks">${checks(o)}</div>${M.ready(o)?btn('Release & dispatch','dispatch-confirm',o.id,'primary','truck'):btn(o.dispatched?'View dispatch record':'View hold details','order',o.id,'','chevron')}</section>`).join('')}</div>${!shown.length?`<section class="card">${empty(tab==='all'?'No orders ready yet':'No orders in this queue',tab==='all'?'Finalize logistics charges and verify payment to clear orders for dispatch.':'Orders will appear here as their status changes.')}</section>`:''}`;}
  function connections(){return `${heading('Connections','See how Shopify, payments and warehouse operations work together.')}<div class="callout">${icon('info')}<p><strong>Interactive prototype with sample data.</strong> Connections and payments are simulated locally. No store access, messages, or real payments are involved.</p></div><div class="integration-grid"><section class="card integration-card"><div class="integration-icon"><span class="shopify-mark" style="font-size:30px">S</span></div><div class="batch-top"><h2>Shopify</h2>${badge('Demo connected')}</div><p>Product orders and payments flow into Seye operations. Approved logistics charges are linked to the original order.</p><dl class="definition-grid"><div><dt>Sample store</dt><dd>Preorder with Seye</dd></div><div><dt>Last demo sync</dt><dd>${state.lastSync}</dd></div><div><dt>Sample orders</dt><dd>${state.orders.length} orders</dd></div><div><dt>Fulfillment control</dt><dd>Payment holds enabled</dd></div></dl>${btn('Sync sample orders','sync','','primary','refresh')}</section><section class="card integration-card"><div class="integration-icon" style="background:var(--blue-soft);color:var(--blue)">${icon('wallet')}</div><div class="batch-top"><h2>Payments</h2>${badge('Simulation mode')}</div><p>Review a logistics invoice and simulate a verified full or partial payment. The order balance and dispatch queue update together.</p><dl class="definition-grid"><div><dt>Currency</dt><dd>NGN · Nigerian naira</dd></div><div><dt>Gateway example</dt><dd>Paystack</dd></div><div><dt>Real collections</dt><dd>Disabled in prototype</dd></div><div><dt>Charge timing</dt><dd>After actual costs are known</dd></div></dl>${btn('Open logistics invoices','nav','billing','','arrow')}</section></div><section class="card help-card"><h3>Try the complete workflow</h3><p>Open the June shipment, approve its actual costs, create a logistics invoice for Amaka Okafor (#1048), simulate a verified payment, then release the order in Dispatch.</p>${btn('Start with June shipment','batch','SHP-2606','primary','ship')}</section><section class="card help-card"><div class="batch-top"><div><h3>Reset the demonstration</h3><p style="margin-bottom:0">Restore sample orders, cost reviews, invoices and payments on this browser.</p></div>${btn('Reset sample data','reset-confirm','','danger','refresh')}</div></section>`;}
  function render(){document.title=`${names[view]} · Seye operations`;$('#app').innerHTML=`<div class="app-shell"><aside class="sidebar"><div class="brand"><div class="brand-mark">s</div><span class="brand-word">seye</span><span class="brand-sub">Operations<br>workspace</span></div><div class="workspace-select"><span class="workspace-icon">${icon('grid','small')}</span><select id="business-select" aria-label="Filter by business"><option value="all">All businesses</option>${Object.entries(M.businesses).map(([k,v])=>`<option value="${k}" ${business===k?'selected':''}>${v}</option>`).join('')}</select>${icon('down','small')}</div><div class="nav-label">Workspace</div><nav aria-label="Main navigation">${nav()}</nav><div class="sidebar-footer"><button class="nav-item ${view==='connections'?'active':''}" data-action="nav" data-id="connections">${icon('link')}Connections</button><button class="nav-item" data-action="guide">${icon('help')}Prototype guide</button><div class="connection-mini"><strong><span class="shopify-mark">S</span>Shopify <span class="status green" style="font-size:8px;margin-left:auto">Demo</span></strong><p>${state.lastSync} · ${state.orders.length} sample orders</p><button data-action="sync">Sync sample orders</button></div><div class="profile"><span class="avatar owner">SO</span><span><strong>Seye Operations</strong><small>Workspace owner</small></span>${icon('down','small')}</div></div></aside><div class="workspace"><header class="topbar"><button class="icon-button mobile-toggle" data-action="mobile-menu" aria-label="Toggle navigation">${icon('menu')}</button><div class="breadcrumbs">${icon('grid','small')}<span>Workspace</span><span>/</span><strong>${names[view]}</strong></div><div class="top-actions"><form id="global-search" class="search-global">${icon('search','small')}<input id="global-input" aria-label="Search all orders" placeholder="Search orders, customers…"><span class="keycap">/</span></form><span class="prototype-label">Prototype · sample data</span><button class="icon-button" data-action="notifications" aria-label="View recent activity">${icon('bell')}<span class="notification-dot"></span></button></div></header><main id="main" class="main" tabindex="-1">${({overview,orders,shipments,inventory,billing,dispatch:dispatchView,connections}[view])()}<footer class="prototype-footer"><span>Seye operations · All three businesses, one workspace</span><a href="README.md" target="_blank">Prototype notes</a></footer></main></div></div>`;}
  function open(html,drawer=false){lastFocus=document.activeElement;$('#app').inert=true;document.body.style.overflow='hidden';$('#overlay-root').innerHTML=`<div class="overlay ${drawer?'drawer-overlay':''}">${html}</div>`;requestAnimationFrame(()=>{$('#overlay-root [data-action="close"]')?.focus();});}
  function close(){const target=lastFocus;$('#overlay-root').innerHTML='';$('#app').inert=false;document.body.style.overflow='';if(target?.isConnected)target.focus();else $('#main')?.focus();}
  function modalHeader(title,subtitle){return `<div class="modal-header"><div><h2 id="dialog-title">${title}</h2>${subtitle?`<p>${subtitle}</p>`:''}</div><button class="icon-button" data-action="close" aria-label="Close dialog">${icon('close')}</button></div>`;}
  function orderDetail(id){const o=get(id),p=product(o);open(`<section class="drawer" role="dialog" aria-modal="true" aria-labelledby="dialog-title">${modalHeader('Order #'+id,'Shopify preorder · '+M.businesses[o.business])}<div class="drawer-body"><div class="drawer-title-row">${customer(o)}${badge(M.status(o))}</div><div class="product-summary">${thumb(p)}<div><p>${p.name}</p><small>${p.variant} · Qty ${o.quantity}</small></div><span class="money">${money(o.productPaid)}</span></div><div class="workflow-steps"><div class="workflow-step done">Product paid</div><div class="workflow-step ${o.received?'done':'active'}">Stock received</div><div class="workflow-step ${M.assessed(o)&&M.balance(o)===0?'done':'active'}">Logistics paid</div><div class="workflow-step ${o.dispatched?'done':''}">Dispatched</div></div><div class="fee-row"><span>Product payment<small>Collected in Shopify</small></span>${badge('Paid')}</div><section class="drawer-section"><h3>Logistics charges</h3><div class="fee-row"><span>Importation<small>${o.batch} · Actual batch costs</small></span><strong>${o.importFee===null?'Not assessed':money(o.importFee)}</strong></div><div class="fee-row"><span>Local delivery<small>${o.city}</small></span><strong>${o.delivery===null?'Not quoted':money(o.delivery)}</strong></div><div class="fee-row"><span>Logistics paid</span><strong class="number-good">${money(o.paid)}</strong></div><div class="fee-row total"><span>Outstanding</span><strong>${M.assessed(o)?money(M.balance(o)):'Pending assessment'}</strong></div>${o.importFee===null?`<div class="callout amber">${icon('info')}<p>Assess the actual shipment costs before creating a customer invoice.</p></div>${btn('Review shipment costs','batch',o.batch,'full primary','invoice')}`:btn(o.invoiced?'View logistics invoice':'Create logistics invoice','invoice',id,'full primary','invoice')}</section><section class="drawer-section"><h3>Dispatch clearance</h3>${checks(o)}<div class="drawer-actions"><button class="btn ${M.ready(o)?'primary':''} full" data-action="dispatch-confirm" data-id="${id}" ${!M.ready(o)?'disabled':''}>${icon('truck')}${o.dispatched?'Already dispatched':M.ready(o)?'Release & dispatch':'Dispatch on hold'}</button></div><p class="detail-note">${o.dispatched?'Dispatch is recorded in the order activity.':'The original product payment does not release this order.'}</p></section><section class="drawer-section"><h3>Order activity</h3><ul class="drawer-history">${[...o.history].reverse().map(h=>`<li>${esc(h.text)}<small>${h.time}</small></li>`).join('')}</ul></section></div></section>`,true);}
  function batchModal(id){const b=state.batches.find(b=>b.id===id),os=state.orders.filter(o=>o.batch===id);if(b.status==='In transit'||b.finalized){open(`<section class="modal narrow" role="dialog" aria-modal="true" aria-labelledby="dialog-title">${modalHeader(b.name,b.id+' · '+b.status)}<div class="modal-body"><div class="callout ${b.finalized?'green':''}">${icon(b.finalized?'checkCircle':'ship')}<p>${b.finalized?'Actual import costs are approved. Customer delivery quotes are managed per order.':'This shipment is still in transit. Actual import costs can be approved once the shipment arrives.'}</p></div><dl class="definition-grid"><div><dt>Origin</dt><dd>${b.origin}</dd></div><div><dt>Destination</dt><dd>${b.destination}</dd></div><div><dt>Freight</dt><dd>${b.freight===null?'Not finalized':money(b.freight)}</dd></div><div><dt>Clearance</dt><dd>${b.clearance===null?'Not finalized':money(b.clearance)}</dd></div><div><dt>Extra stock</dt><dd>${b.extraUnits} units</dd></div><div><dt>Arrival</dt><dd>${b.date}</dd></div></dl>${b.businessAllocation!==undefined?`<div class="fee-row total"><span>Business-owned stock allocation</span><strong>${money(b.businessAllocation)}</strong></div>`:''}<p class="detail-note">${b.finalized?'Approved costs are kept with this shipment. Later tariff changes do not rewrite them.':'Customers have paid for products only. Their logistics charges remain unassessed.'}</p></div><div class="modal-footer">${btn('Close','close')}${b.finalized?btn('View customer charges','billing-unassessed','','primary'):''}</div></section>`);return;}
    open(`<section class="modal" role="dialog" aria-modal="true" aria-labelledby="dialog-title">${modalHeader('Review actual import costs',b.name+' · '+b.id)}<form id="batch-form" data-id="${id}"><div class="modal-body"><div class="callout">${icon('info')}<p>Enter the confirmed shipment costs in NGN. Customers are allocated by chargeable weight; extra stock keeps its own share.</p></div><div class="two-column"><div><label class="field"><span>International freight</span><div class="input-prefix"><em>₦</em><input name="freight" id="freight-input" type="number" min="0" step="1" required value="${b.freight}"></div><small>Actual cost paid for this shipment</small></label><label class="field"><span>Clearance & import handling</span><div class="input-prefix"><em>₦</em><input name="clearance" id="clearance-input" type="number" min="0" step="1" required value="${b.clearance}"></div></label><label class="field"><span>Cost record date</span><input type="date" name="date" value="2026-09-13" required></label><label class="field"><span>Supplier / agent bill reference</span><input name="reference" value="FRT-2606-018" required maxlength="80"></label><div class="accent-line"><h3>Local delivery is separate</h3><p>Add the current delivery quote when creating each customer's invoice.</p></div></div><div><div class="wide-label"><span>Allocation preview</span><small class="muted">${os.length} customer orders</small></div><div id="allocation-preview"></div><p class="detail-note">${b.extraWeight} kg belongs to extra business stock. Rounding is reconciled to the business allocation.</p></div></div><p class="form-error" id="form-error" role="alert"></p></div><div class="modal-footer"><span class="note">Approves sample charges. No customer is contacted.</span>${btn('Cancel','close')}<button class="btn primary" type="submit">${icon('check')}Approve actual costs</button></div></form></section>`);updateAllocation();}
  function updateAllocation(){const form=$('#batch-form');if(!form)return;const b=state.batches.find(b=>b.id===form.dataset.id),os=state.orders.filter(o=>o.batch===b.id),sum=Number(form.freight.value)+Number(form.clearance.value),weight=os.reduce((s,o)=>s+o.weight,0)+b.extraWeight;let allocated=0;$('#allocation-preview').innerHTML=`<div class="allocation-preview"><table><thead><tr><th>Allocation</th><th>Weight</th><th>Import charge</th></tr></thead><tbody>${os.map(o=>{const fee=Math.round(sum*o.weight/weight);allocated+=fee;return `<tr><td>#${o.id} · ${o.name.split(' ')[0]}</td><td>${o.weight} kg</td><td class="money">${money(fee)}</td></tr>`;}).join('')}<tr class="allocation-extra"><td>Extra business stock</td><td>${b.extraWeight} kg</td><td class="money">${money(sum-allocated)}</td></tr><tr><td colspan="2"><strong>Total shipment cost</strong></td><td class="money">${money(sum)}</td></tr></tbody></table></div>`;}
  function invoiceModal(id){const o=get(id);if(o.importFee===null){batchModal(o.batch);return;}const existing=o.invoiced;open(`<section class="modal" role="dialog" aria-modal="true" aria-labelledby="dialog-title">${modalHeader(existing?'Logistics invoice':'Create logistics invoice','Order #'+id+' · '+o.name)}<form id="invoice-form" data-id="${id}"><div class="modal-body"><div class="two-column"><div>${existing?`<div class="callout ${M.balance(o)===0?'green':'amber'}">${icon(M.balance(o)===0?'checkCircle':'clock')}<p>${M.balance(o)===0?'This logistics invoice is fully paid.':money(M.balance(o))+' remaining. Dispatch stays on hold until payment is verified.'}</p></div>`:`<div class="callout">${icon('checkCircle')}<p>Actual import costs approved for ${o.batch}. Add the final delivery quote below.</p></div>`}<label class="field"><span>Customer</span><div class="readonly-field">${o.name}<small>${o.city}</small></div></label><label class="field"><span>Importation charge</span><div class="readonly-field">${money(o.importFee)}<small>Approved batch allocation</small></div></label><label class="field"><span>Local delivery charge</span><div class="input-prefix"><em>₦</em><input id="delivery-input" name="delivery" type="number" min="0" step="1" required value="${o.delivery===null?(o.city.startsWith('Lagos')?5000:8500):o.delivery}" ${existing?'readonly':''}></div><small>Use the current carrier quote. Enter 0 for confirmed pickup.</small></label><label class="field"><span>Delivery quote valid until</span><input name="validUntil" type="date" value="${o.quoteValidUntil}" min="2026-09-13" ${existing?'readonly':''} required></label><p class="form-error" id="form-error" role="alert"></p><p class="detail-note">${existing?'This issued invoice is preserved. A future cost change would need a recorded revision.':'Review the itemized bill before issuing it. Product payment is already settled.'}</p></div><div id="invoice-preview"></div></div></div><div class="modal-footer"><span class="note">Demo invoice. No email or real payment request is sent.</span>${btn('Close','close')}${existing?(M.balance(o)>0?btn('Simulate payment','payment',id,'primary','wallet'):btn('View order','order',id,'primary','arrow')):'<button class="btn primary" type="submit">Issue demo invoice '+icon('arrow')+'</button>'}</div></form></section>`);updateInvoice();}
  function updateInvoice(){const f=$('#invoice-form');if(!f)return;const o=get(f.dataset.id),delivery=Number(f.delivery.value),total=o.importFee+delivery;$('#invoice-preview').innerHTML=`<div class="receipt"><div class="receipt-top"><span class="brand-word">seye</span><span class="status gray">${o.invoiced?'Issued':'Preview'}</span></div><h3>Logistics invoice</h3><p class="customer-address">LOG-${o.id} · Linked to Shopify order #${o.id}<br>Bill to <strong>${o.name}</strong><br>${o.city}</p><div class="fee-row"><span>Importation<small>${o.batch}</small></span><strong>${money(o.importFee)}</strong></div><div class="fee-row"><span>Local delivery</span><strong>${money(delivery)}</strong></div><div class="fee-row total"><span>Logistics total</span><strong style="font-size:15px">${money(total)}</strong></div><div class="fee-row"><span>Logistics payment received</span><strong>${money(o.paid)}</strong></div><div class="receipt-total"><span>Amount due</span><strong>${money(Math.max(0,total-o.paid))}</strong></div><p class="receipt-foot">Product payment of ${money(o.productPaid)} has already been received.<br>Goods are released after all required payments are verified.</p></div>`;}
  function paymentModal(id){const o=get(id);open(`<section class="modal narrow" role="dialog" aria-modal="true" aria-labelledby="dialog-title">${modalHeader('Simulate verified payment','LOG-'+id+' · '+o.name)}<form id="payment-form" data-id="${id}"><div class="modal-body"><div class="callout">${icon('info')}<p>This simulates a successful payment confirmation. No money moves and no payment service is contacted.</p></div><div class="fee-row total" style="border:0;padding:0"><span>Balance outstanding</span><strong>${money(M.balance(o))}</strong></div><label class="field"><span>Payment amount</span><div class="input-prefix"><em>₦</em><input name="amount" type="number" min="1" max="${M.balance(o)}" step="1" value="${M.balance(o)}" required></div><small>A partial payment keeps dispatch on hold.</small></label><label class="field"><span>Sample payment reference</span><input name="reference" value="DEMO-${id}-${Date.now().toString().slice(-6)}" required maxlength="80"></label><p class="form-error" id="form-error" role="alert"></p></div><div class="modal-footer">${btn('Cancel','close')}<button class="btn primary" type="submit">${icon('checkCircle')}Simulate verified payment</button></div></form></section>`);}
  function dispatchConfirm(id){const o=get(id);if(!M.ready(o)){toast('Order is on hold. Finalize all charges and verify payment first.');return;}open(`<section class="modal narrow" role="dialog" aria-modal="true" aria-labelledby="dialog-title">${modalHeader('Release order #'+id,o.name+' · '+o.city)}<div class="modal-body"><div class="callout green">${icon('checkCircle')}<p>All payment and stock checks have passed. This order can leave the Lagos warehouse.</p></div>${checks(o)}<p class="detail-note">Confirming updates the sample order and inventory. No courier booking is made.</p></div><div class="modal-footer">${btn('Cancel','close')}${btn('Confirm dispatch','do-dispatch',id,'primary','truck')}</div></section>`);}
  function activity(text,detail,type='check'){state.activity.unshift({text,detail,type,time:'Just now'});state.activity=state.activity.slice(0,20);}
  function exportCSV(){const rows=[['Order','Customer','Business','Product paid NGN','Import fee NGN','Delivery NGN','Logistics paid NGN','Balance NGN','Status'],...all().map(o=>[o.id,o.name,M.businesses[o.business],o.productPaid,o.importFee===null?'Not assessed':o.importFee,o.delivery===null?'Not quoted':o.delivery,o.paid,M.assessed(o)?M.balance(o):'Not assessed',M.status(o)])];const blob=new Blob([rows.map(r=>r.map(v=>'"'+String(v).replaceAll('"','""')+'"').join(',')).join('\r\n')],{type:'text/csv;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='seye-sample-orders.csv';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);toast('Sample orders exported.');}
  function guide(){open(`<section class="modal narrow" role="dialog" aria-modal="true" aria-labelledby="dialog-title">${modalHeader('Explore the prototype','A complete preorder-to-dispatch scenario')}<div class="modal-body"><div class="callout">${icon('info')}<p>All records are fictional. Changes are saved only in this browser.</p></div><ol class="connection-list"><li>Review the June shipment's actual costs.</li><li>Approve the allocation, including extra stock.</li><li>Create Amaka's invoice, order #1048.</li><li>Add her current delivery quote.</li><li>Simulate a full or partial verified payment.</li><li>Release her order when all checks pass.</li></ol><p class="detail-note">Use the business selector to explore importation, fashion and kitchen stock. Reset the demonstration under Connections.</p></div><div class="modal-footer">${btn('Close','close')}${btn('Review June costs','batch','SHP-2606','primary','arrow')}</div></section>`);}
  document.addEventListener('click',e=>{if(e.target.classList.contains('overlay')){close();return;}const b=e.target.closest('[data-action]');if(!b)return;const a=b.dataset.action,id=b.dataset.id;
    if(a==='nav'){close();go(id);}if(a==='stat'){go(id,b.dataset.filter);}if(a==='order')orderDetail(id);if(a==='batch')batchModal(id);if(a==='invoice')invoiceModal(id);if(a==='payment')paymentModal(id);if(a==='close')close();if(a==='guide')guide();if(a==='dispatch-confirm')dispatchConfirm(id);if(a==='tab'){tab=id;render();}if(a==='billing-unassessed'){close();go('billing','unassessed');}if(a==='billing-unpaid'){go('billing','unpaid');}if(a==='mobile-menu')$('.sidebar').classList.toggle('mobile-open');if(a==='export')exportCSV();
    if(a==='sync'){state.syncs++;state.lastSync='Just now';activity('Sample Shopify orders synchronized',state.orders.length+' orders checked · No duplicates','refresh');save();render();toast('Demo sync complete. '+state.orders.length+' sample orders checked; no duplicates added.');}
    if(a==='notifications')open(`<section class="drawer" role="dialog" aria-modal="true" aria-labelledby="dialog-title">${modalHeader('Recent activity','Changes across this demonstration')}<div class="drawer-body"><div class="activity-list">${activityMarkup(20)}</div></div></section>`,true);
    if(a==='do-dispatch'){try{M.dispatch(state,id);activity('Order #'+id+' dispatched',get(id).name+' · All charges verified','truck');save();close();render();toast('Order #'+id+' dispatched. Sample inventory updated.');}catch(err){toast(err.message);}}
    if(a==='reset-confirm')open(`<section class="modal narrow" role="dialog" aria-modal="true" aria-labelledby="dialog-title">${modalHeader('Reset sample data','Restore the starting demonstration')}<div class="modal-body"><p>Your local sample invoices, payments, approvals and dispatches will be replaced with the original demo records.</p></div><div class="modal-footer">${btn('Keep changes','close')}${btn('Reset sample data','reset','','primary','refresh')}</div></section>`);
    if(a==='reset'){state=M.seed();save();close();go('overview');toast('Sample data restored.');}
  });
  document.addEventListener('change',e=>{if(e.target.id==='business-select'){business=e.target.value;query='';render();}if(e.target.id==='table-sort'){sort=e.target.value;render();}});
  document.addEventListener('input',e=>{if(e.target.id==='table-search'){query=e.target.value;$('#table-results').innerHTML=view==='billing'?billingTable(filterOrders()):orderTable(filterOrders());$('#result-count').textContent=filterOrders().length+' orders';}if(['freight-input','clearance-input'].includes(e.target.id))updateAllocation();if(e.target.id==='delivery-input')updateInvoice();});
  document.addEventListener('submit',e=>{e.preventDefault();const f=e.target;if(f.id==='global-search'){const q=f.querySelector('input').value;go('orders');query=q;render();return;}try{
    if(f.id==='batch-form'){const b=M.allocate(state,f.dataset.id,Number(f.freight.value),Number(f.clearance.value));b.costDate=f.date.value;b.billReference=f.reference.value;activity('Actual costs approved for '+b.id,money(b.freight+b.clearance)+' allocated to orders and extra stock','invoice');save();close();render();toast('Actual import costs approved. Add delivery quotes to create invoices.');}
    if(f.id==='invoice-form'){const o=M.invoice(state,f.dataset.id,Number(f.delivery.value));o.quoteValidUntil=f.validUntil.value;activity('Logistics invoice created for '+o.name,money(M.total(o))+' · LOG-'+o.id,'invoice');save();close();render();invoiceModal(o.id);toast('Demo invoice issued. No message was sent.');}
    if(f.id==='payment-form'){const o=M.pay(state,f.dataset.id,Number(f.amount.value),f.reference.value.trim());activity('Demo payment verified for '+o.name,money(Number(f.amount.value))+' · LOG-'+o.id,'check');save();close();render();orderDetail(o.id);toast(M.ready(o)?'Payment verified. Order #'+o.id+' is now ready to dispatch.':'Partial payment recorded. '+money(M.balance(o))+' remains due.');}
  }catch(err){const error=$('#form-error');if(error)error.textContent=err.message;else toast(err.message);}});
  document.addEventListener('keydown',e=>{if(e.key==='Escape'){close();$('.sidebar')?.classList.remove('mobile-open');}if(e.key==='/'&&!['INPUT','TEXTAREA','SELECT'].includes(document.activeElement?.tagName)&&!$('#overlay-root').children.length){e.preventDefault();$('#global-input')?.focus();}if(e.key==='Tab'&&$('#overlay-root').children.length){const nodes=[...$('#overlay-root').querySelectorAll('button:not([disabled]),input:not([disabled]),select,a[href],[tabindex="0"]')].filter(n=>n.offsetParent!==null);if(!nodes.length)return;const first=nodes[0],last=nodes[nodes.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus();}}});
  window.addEventListener('hashchange',()=>{const next=location.hash.slice(1);if(views.includes(next)&&next!==view){view=next;tab='all';query='';close();render();}});
  render();
})();
